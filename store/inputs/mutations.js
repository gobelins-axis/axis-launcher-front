// Config
import placeholderGame from '@/config/placeholder-game';

const min = 12;

const mutations = {
    SET_INPUTS(state, inputs) {
        state.inputs = inputs;
    },
};

export default mutations;
