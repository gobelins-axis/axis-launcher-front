const getters = {
    previous(state) {
        return state.previous;
    },

    current(state) {
        return state.current;
    },

    state(state) {
        return state;
    },
};

export default getters;
